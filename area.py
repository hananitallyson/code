width = float(input("\nEnter the width in meters (e.g. 20): "))
length = float(input("Enter the length in meters (e.g. 30): "))

area = width * length

print(f"The area of {width} x {length} is equal to {area:.2f} m².\n")
